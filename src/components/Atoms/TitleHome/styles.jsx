import styled from "styled-components";

export const Container = styled.div`
  color: white;
  font-size: 4rem;
  font-weight: normal;
  margin-bottom: 20px;
  text-align: left;
`;

export default Container;
